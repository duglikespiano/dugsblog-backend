import contactDao from '../models/contactDao';

const fetchAllMessages = async (name: string, email: string, message: string) => {
	// await contactDao.fetchAllMessages(string);
};
export default { fetchAllMessages };
